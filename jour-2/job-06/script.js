/* 
    Nous allons écouter l'élement Window 
    si keypress (appuye sur une touche) alors (Event e) => fonction(Event e)
  */
window.addEventListener(
  "keypress",
  (e) => /* Ajouter la fonction ICI */ addFunction(e) // remplacer addFunction
); // Ecouteur
